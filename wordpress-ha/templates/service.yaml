apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordpress-ha.fullname" . }}
  labels:
    {{- include "wordpress-ha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordpress-ha.selectorLabels" . | nindent 4 }}
