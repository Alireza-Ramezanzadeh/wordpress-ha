apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "wordpress-ha.master.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.master.storage.size }}
  storageClassName: {{ .Values.master.storage.storageClass }}
  